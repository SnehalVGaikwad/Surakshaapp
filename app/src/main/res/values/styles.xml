<resources>

    <!-- Base application theme. -->
    <style name="Theme.SurakshaApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/accent_pink</item>
        <item name="colorPrimaryVariant">@color/accent_pink_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/accent_pink</item>
        <item name="colorSecondaryVariant">@color/accent_pink_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Background Color -->
        <item name="android:windowBackground">@color/pastel_pink</item>
        <!-- Customize your theme here. -->
    </style>

</resources>
